openapi: 3.0.3
info: {title: API, version: "1.0"}
paths:
  /users/{id}:
    parameters:
      - {name: id, in: path, required: true, schema: {type: string}}
    get:
      responses:
        "200": {description: ok}
