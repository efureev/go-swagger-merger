openapi: 3.0.3
info: {title: API, version: "1.0"}
paths:
  /x:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema: {$ref: "#/components/schemas/Used"}
components:
  schemas:
    Used: {type: object}
    Orphan: {type: object}
